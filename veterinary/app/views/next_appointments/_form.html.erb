<%= form_for(@next_appointment) do |f| %>
  <% if @next_appointment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@next_appointment.errors.count, "error") %> prohibited this next_appointment from being saved:</h2>

      <ul>
      <% @next_appointment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date_of_appointment %><br>
    <%= f.text_field :date_of_appointment %>
  </div>
  <div class="field">
    <%= f.label :reason_for_visit %><br>
    <%= f.text_field :reason_for_visit %>
  </div>
  <div class="field">
    <%= f.label :appointment_id %><br>
    <%= f.text_field :appointment_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
